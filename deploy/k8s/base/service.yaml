apiVersion: v1
kind: Service
metadata:
  name: launchpad
  labels:
    app: launchpad
    component: api
spec:
  type: ClusterIP
  selector:
    app: launchpad
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
    name: http
